interface:
  display_name: "Article Image Generate"
  short_description: "Generate a hero and 2–3 inline article images."
  default_prompt: "Use $article-image-generate with $imagegen to read the supplied _articles source in full, generate three or four context-specific images, set the hero through frontmatter, and place the remaining images at meaningful narrative points."
